<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Rail Madad User Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" href="/images/train.png" type="image/png" />

  </head>
  <body class="bg-gray-100 min-h-screen flex flex-col">
    <!-- Sidebar -->
    <%- include('../sideBars/userSide.ejs') %>

    <!-- Main Content Area -->
    <main class="flex-1 p-6">
      <section id="submit-Issue" class="w-full max-w-5xl mx-auto">
        <div class="bg-white p-10 rounded-lg shadow-lg w-full">
          <h1 class="text-4xl font-bold text-gray-800 mb-8 text-center">
            Submit an Issue
          </h1>
          <form
            id="IssueForm"
            enctype="multipart/form-data"
            class="space-y-6"
            method="post"
            action="/issues"
          >
            <!-- Issue Title -->
            <div>
              <label for="title" class="block text-lg font-medium text-gray-700"
                >Issue Title:</label
              >
              <input
                type="text"
                id="title"
                name="title"
                class="mt-2 p-4 block w-full rounded-md border border-gray-300 focus:ring-purple-500 focus:border-purple-500 shadow-sm"
                placeholder="Enter the title of your issue"
                required
              />
            </div>

            <!-- Issue Description -->
            <div>
              <label for="desc" class="block text-lg font-medium text-gray-700"
                >Issue Description:</label
              >
              <textarea
                id="desc"
                name="desc"
                rows="6"
                class="mt-2 p-4 block w-full rounded-md border border-gray-300 focus:ring-purple-500 focus:border-purple-500 shadow-sm"
                placeholder="Enter the description of your issue"
                required
              ></textarea>
            </div>
            <!--Issue location-->
            <div>
              <label
                for="stationOrTrain"
                class="block text-lg font-medium text-gray-700"
                >Issue Station or Issue Train details</label
              >
              <textarea
                id="stationOrTrain"
                name="stationOrTrain"
                rows="6"
                class="mt-2 p-4 block w-full rounded-md border border-gray-300 focus:ring-purple-500 focus:border-purple-500 shadow-sm"
                placeholder="Enter the station or Train where the issue happened"
                required
              ></textarea>
            </div>
            <div>
              <label
                for="platform"
                class="block text-lg font-medium text-gray-700"
                >Issue platform: (optional)</label
              >
              <textarea
                id="platform"
                name="platform"
                rows="6"
                class="mt-2 p-4 block w-full rounded-md border border-gray-300 focus:ring-purple-500 focus:border-purple-500 shadow-sm"
                placeholder="Enter the platform where the issue is there"
                value=""
              ></textarea>
            </div>
            <div>
              <label for="pnrNo" class="block text-lg font-medium text-gray-700"
                >Issue PNR No:</label
              >
              <textarea
                id="pnrNo"
                name="pnrNo"
                rows="6"
                class="mt-2 p-4 block w-full rounded-md border border-gray-300 focus:ring-purple-500 focus:border-purple-500 shadow-sm"
                placeholder="Enter the PNR number of your issue"
                required
              ></textarea>
            </div>

            <!-- Media Upload -->
            <div>
              <label
                for="mediaType"
                class="block text-lg font-medium text-gray-700"
                >Select Media Type:</label
              >
              <select
                id="mediaType"
                name="mediaType"
                class="mt-2 block w-full text-lg text-gray-700 p-3 rounded-md border border-gray-300"
                required
              >
                <option value="image">Image</option>
                <option value="audio">Audio</option>
                <option value="video">Video</option>
              </select>
            </div>

            <div class="mt-4">
              <label for="media" class="block text-lg font-medium text-gray-700"
                >Upload Media:</label
              >
              <input
                type="file"
                id="media"
                name="media"
                accept="image/*,audio/*,video/*"
                class="mt-2 block w-full text-lg text-gray-500 file:mr-4 file:py-3 file:px-6 file:rounded-full file:border-0 file:text-lg file:font-semibold file:bg-purple-100 file:text-purple-700 hover:file:bg-purple-200"
                multiple
                required
              />
            </div>

            <!-- Submit Button -->
            <button
              type="submit"
              class="w-full bg-purple-600 text-white font-semibold py-4 rounded-lg shadow-lg hover:bg-purple-700 transition duration-300 ease-in-out text-lg"
            >
              Submit Issue
            </button>
          </form>

          <!-- Response Div -->
          <div
            id="response"
            class="mt-6 text-center text-lg text-gray-700"
          ></div>
        </div>
      </section>
    </main>
    <%-include('../footer.ejs')%>
  </body>
</html>
